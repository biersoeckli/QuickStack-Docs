{
  "name": "quickstack-docs-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @quickstack/fumadocs",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspace @quickstack/fumadocs"
  }
}
